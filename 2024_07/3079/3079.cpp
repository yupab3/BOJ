#include <iostream>

using namespace std;

ssize_t t[100005];
int	main()
{
	ios::sync_with_stdio(0);
	std::cin.tie(0);

	ssize_t n, m, tmp;
	cin >> n >> m;
	for (ssize_t i = 1; i <= n; i++)
		cin >> t[i];
	
}